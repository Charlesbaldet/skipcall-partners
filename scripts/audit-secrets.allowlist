# audit-secrets allow-list
# One regex per line. Lines matching either the relative file path
# or the file:line locator are skipped. Blank lines and lines
# starting with '#' are ignored.
#
# Empty by default — current main is expected to be green.
